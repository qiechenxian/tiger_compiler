	.align  2
	.global main
	.type   main, %function
main:
	stmfd   sp!, {fp, lr}
	add     FP, SP, #4
	sub     SP, SP, #4
	ldr     106, =a
	mov     R10, #10
	str     R10, [106]
	ldr     R10, =a
	ldr     R0, [R10]
	mov     R10, #0
	cmp     R0, R10
	bgt     .L0
	b       .L1
.L1:
	mov     R10, #0
	mov     R0, R10
.L2:
	b       .L3
.L0:
	mov     R10, #1
	mov     R0, R10
	b       .L2
.L3:
NOP
	sub     sp, fp, #4
	ldmfd   sp!, {fp, lr}
	bx      lr
	.size   main, .-main