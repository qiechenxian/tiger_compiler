	.align  2
	.global main
	.type   main, %function
main:
	stmfd   sp!, {fp, lr}
	add     FP, SP, #4
	sub     SP, SP, #4
	ldr     R1, =a
	mov     R10, #0
	mov     R0, #4
	mul     R10, R10, R0
	add     105, R1, R10
	mov     R10, #1
	str     R10, [105]
	mov     R10, #0
	mov     R0, R10
	b       .L0
.L0:
NOP
	sub     sp, fp, #4
	ldmfd   sp!, {fp, lr}
	bx      lr
	.size   main, .-main