	.arch   armv7-a
	.file   "/mnt/e/workspace_clion/compiler_2020/sysyruntimelibrary-master/functional_test/36_domain_test.sy"
	.data
	.comm   a, 8, 4
	.text
	.align  2
	.global func
	.type   func, %function
func:
	str     fp, [sp, #-4]!
	add     FP, SP, #0
	sub     SP, SP, #4
	ldr     R1, =a
	mov     R10, #0
	mov     R0, #4
	mul     R10, R10, R0
	add     R0, R1, R10
	mov     R10, #1
	str     R10, [R0]
	add     R1, FP, #4
	ldr     R10, =a
	mov     R9, #0
	mov     R0, #4
	mul     R0, R9, R0
	add     R10, R10, R0
	ldr     R10, [R10]
	sub     R10, R10, #3
	mov     R0, #4
	mul     R10, R10, R0
	add     R10, R1, R10
	ldr     R10, [R10]
	mov     R0, R10
	b       .L0
.L0:
NOP
	sub     sp, fp, #0
	ldr     fp, [sp], #4
	bx      lr
	.size   func, .-func
	.align  2
	.global main
	.type   main, %function
main:
	stmfd   sp!, {fp, lr}
	add     FP, SP, #4
	sub     SP, SP, #20
	add     R9, FP, #-20
	mov     R1, #0
	mov     R10, #4
	mul     R1, R1, R10
	add     R10, R9, R1
	mov     R1, #-1
	str     R1, [R10]
	add     R9, FP, #-20
	mov     R1, #1
	mov     R10, #4
	mul     R1, R1, R10
	add     R10, R9, R1
	mov     R1, #4
	str     R1, [R10]
	add     R9, FP, #-20
	mov     R1, #2
	mov     R10, #4
	mul     R1, R1, R10
	add     R10, R9, R1
	mov     R1, #8
	str     R1, [R10]
	add     R1, FP, #-8
# 	mov     R1, R1
	add     R2, FP, #-20
	str     R2, [SP]
	bl      func
	mov     R10, R0
	str     R10, [R1]
	ldr     R10, [FP, #-8]
	add     R8, FP, #-20
	mov     R0, #1
	mov     R9, #4
	mul     R0, R0, R9
	add     R0, R8, R0
	ldr     R0, [R0]
	add     R10, R10, R0
	mov     R0, R10
	b       .L1
.L1:
NOP
	sub     sp, fp, #4
	ldmfd   sp!, {fp, lr}
	bx      lr
	.size   main, .-main
